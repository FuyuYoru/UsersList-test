# React + TypeScript + Vite(сборщик) + RTK(для запросов и хранения данных) + SCSS(стили) + FSD(для удобства)

# Для запуска проекта нужно установить пакеты с помощью npm install

# Далее запустить сервер через npm run dev

#

# UsersList-test
